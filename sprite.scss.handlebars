{{#sprites}}
@mixin {{name}}($ratio:3) {
  display:inline-block;
  width: calc({{width}} / $ratio);
  height: calc({{height}} / $ratio);
  background-position: calc({{offset_x}} / $ratio) calc({{offset_y}} / $ratio);
  background-size: calc({{total_width}} / $ratio) calc({{total_height}} / $ratio);
  background-image: url($spriteURL+'{{{escaped_image}}}'+$spriteCache);
  background-repeat: no-repeat;
}
{{/sprites}}
{{#sprites}}
.{{name}} {@include {{name}}();}
{{/sprites}}